<?php

/**
 * @file
 * Main module file.
 */

/**
 * Implements hook_theme().
 */
function properties_field_theme($existing, $type, $theme, $path) {
  return [
    'properties_list' => [
      'variables' => ['items' => []],
    ],
  ];
}
