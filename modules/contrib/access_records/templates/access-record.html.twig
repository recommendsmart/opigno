{#
/**
 * @file
 * Default theme implementation to present an access record entity.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_access_record()
 */
#}
<div{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
