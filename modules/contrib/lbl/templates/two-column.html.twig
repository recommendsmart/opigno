<section {{ settings.section_attributes.addClass('two-column-section') }}>
  <div {{ attributes.addClass(classes).addClass("two-column") }}>
    <div {{ region_attributes.left.addClass('left') }}>
      {{ content.left }}
    </div>
    <div {{ region_attributes.right.addClass('right') }}>
      {{ content.right }}
    </div>
  </div>
</section>
